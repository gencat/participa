default: &default
  # Database configuration
  DB_PORT: "5432"
  DB_POOL: <%= ENV.fetch("RAILS_MAX_THREADS") { "5" } %>
  DB_HOST: <%= ENV.fetch("DATABASE_HOST") { "localhost" } %>
  # Mailer configuration
  MAILER_SMTP_ADDRESS: "smtp email address"
  MAILER_SMTP_DOMAIN: "smtp email domain"
  MAILER_SMTP_PORT: "587"
  MAILER_SMTP_USER_NAME: "smtp email username"
  MAILER_SMTP_PASSWORD: "smtp email password"
  # GeoCoder configuration (developer.here.com)
  GEOCODER_LOOKUP_APP_ID: "here_app_id"
  GEOCODER_LOOKUP_APP_CODE: "here_app_code"
  # Socrata Open Data API, or SODA configuration.
  SODA_DOMAIN: "soda.demo.socrata.com"
  SODA_USERNAME: "demouser@example.com"
  SODA_PASSWORD: "my_demo_user_password"
  SODA_APP_TOKEN: "my_demo_app_token"

development:
  <<: *default
  DB_DATABASE: "participa_gencat_development"
  DB_USERNAME: "admin"
  DB_PASSWORD: "admin"

test:
  <<: *default
  TEST_DB_DATABASE: "participa_gencat_test"
  TEST_DB_USERNAME: "admin"
  TEST_DB_PASSWORD: "admin"

integration:
  <<: *default
  # secrets
  SECRET_KEY_BASE: <%= ENV['SECRET_KEY_BASE'] %>

staging:
  <<: *default

production:
  <<: *default
