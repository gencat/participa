en:
  # Remove when merge: https://github.com/decidim/decidim/pull/11450/files
  decidim:
    admin:
      components:
        component:
          answers_alert: If you publish the component, all answers will be deleted.
