---
en:
  activemodel:
    attributes:
      survey_answer:
        choices: Choices
    errors:
      models:
        survey_answer:
          attributes:
            choices:
              missing: are missing
