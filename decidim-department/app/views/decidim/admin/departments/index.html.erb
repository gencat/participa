<div class="card">
  <div class="card-divider">
    <h2 class="card-title"><%= t "decidim.admin.titles.departments" %> <%= link_to t("actions.add", scope: "decidim.admin"), "/admin/departments/new", class: 'button tiny button--title' if allowed_to? :create, :department %></h2>
  </div>
  <div class="card-section">
    <div class="table-scroll">
        <table class="table-list">
        <thead>
          <tr>
            <th><%= t("models.scope.fields.name", scope: "decidim.admin") %></th>
            <th><%= t("departments.fields.created_at", scope: "decidim.admin") %></th>
            <th><%= t("departments.fields.updated_at", scope: "decidim.admin") %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @departments.each do |department| %>
            <tr>
              <td>
                <%= department.name %><br />
              </td>
              <td>
                <%= l department.created_at, format: :short %>
              </td>
              <td>
                <%= l department.updated_at, format: :short %>
              </td>
              <td class="table-list__actions">
                <% if allowed_to? :update, :department, department: department %>
                  <%= icon_link_to "pencil", "/admin/departments/"+department.id.to_s+"/edit", t("actions.edit", scope: "decidim.admin"), class: "action-icon--edit", method: :get, data: {} %>
                <% end %>
                <% if allowed_to? :destroy, :department, department: department %>
                  <%= icon_link_to "circle-x", "/admin/departments/"+department.id.to_s, t("actions.destroy", scope: "decidim.admin"), class: "action-icon--remove", method: :delete, data: { confirm: t("actions.confirm_destroy", scope: "decidim.admin") } %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
